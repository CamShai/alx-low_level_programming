#include "main.h"
/**
* _abs - outputs the absolute value of an integer
* @n: the int to be checked for absolute value
* Return: absolute value of int
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

